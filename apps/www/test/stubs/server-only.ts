// Stub for the `server-only` package, which throws when resolved outside a
// React Server Component graph. Aliased in vitest.config.ts so server-tagged
// modules can be imported in unit tests.
export {};
