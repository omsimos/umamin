/// <reference types="@cloudflare/vitest-pool-workers/types" />
// Provides the `cloudflare:test` module types (env, createExecutionContext,
// waitOnExecutionContext, …) to the type-checker for the *.worker.test.ts suites.
