const { PrismaClient } = require('@prisma/client');

const prisma = new PrismaClient();

const main = async () => {
  // eslint-disable-next-line no-console
  console.info('TODO: seed data');
};

// eslint-disable-next-line no-console
main().catch(console.error);
