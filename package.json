{
  "name": "umamin",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "db:push": "pnpm --filter @umamin/server db:push",
    "db:migrate": "pnpm --filter @umamin/server db:migrate",
    "db:studio": "pnpm --filter @umamin/server db:studio",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "ui:add": "pnpm --filter @umamin/ui ui:add"
  },
  "devDependencies": {
    "@umamin/eslint-config": "workspace:*",
    "@umamin/typescript-config": "workspace:*",
    "prettier": "^3.2.5",
    "turbo": "^1.13.3"
  },
  "packageManager": "pnpm@9.1.2",
  "engines": {
    "node": ">=20"
  }
}
