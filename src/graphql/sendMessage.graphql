mutation sendMessage($content: String!, $receiverUsername: String!, $senderUsername: String) {
  sendMessage(content: $content, receiverUsername: $receiverUsername, senderUsername: $senderUsername) {
    id
    content
  }
}
