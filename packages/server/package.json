{
  "name": "@umamin/server",
  "main": "./index.ts",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "turso dev --db-file ./db/dev.db",
    "clean": "rm -rf ./node_modules .turbo",
    "check-types": "tsc --noEmit",
    "db:generate": "pnpm drizzle-kit generate",
    "db:migrate": "pnpm drizzle-kit migrate",
    "db:push": "pnpm drizzle-kit push",
    "db:studio": "pnpm drizzle-kit studio"
  },
  "devDependencies": {
    "@umamin/tsconfig": "workspace:*",
    "drizzle-kit": "^0.21.2"
  },
  "dependencies": {
    "@libsql/client": "^0.6.0",
    "@pothos/core": "^3.41.1",
    "@pothos/plugin-scope-auth": "^3.22.0",
    "drizzle-orm": "^0.30.10",
    "graphql": "^16.8.1",
    "graphql-scalars": "^1.23.0",
    "nanoid": "^5.0.7"
  }
}
