{
  "name": "@umamin/server",
  "main": "./index.ts",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf ./node_modules",
    "db:migrate": "cd db && pnpm drizzle-kit generate:sqlite",
    "db:push": "cd db && pnpm drizzle-kit push:sqlite",
    "db:studio": "cd db && pnpm drizzle-kit studio"
  },
  "devDependencies": {
    "@umamin/typescript-config": "workspace:*",
    "drizzle-kit": "^0.20.17",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@libsql/client": "^0.6.0",
    "@pothos/core": "^3.41.1",
    "drizzle-orm": "^0.30.8",
    "graphql-scalars": "^1.23.0",
    "nanoid": "^5.0.7"
  }
}
