{
  "name": "@umamin/db",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "prisma generate && tsc",
    "build": "prisma generate && tsc",
    "clean": "rm -rf dist/ .turbo/ node_modules/",
    "clean:cache": "rm -rf dist/ .turbo/",
    "postinstall": "prisma generate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^4.1.1",
    "prisma": "^4.1.1"
  },
  "devDependencies": {
    "typescript": "^4.7.4"
  }
}
